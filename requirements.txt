# Clinical Trial Intelligence Platform - Streamlit Cloud Compatible
# Python 3.10+

# Core Dependencies
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.1.0
imbalanced-learn>=0.11.0
shap>=0.44.0

# Visualization
plotly>=5.17.0
matplotlib>=3.7.0
seaborn>=0.13.0
altair>=5.2.0

# Utilities
joblib>=1.3.0
tqdm>=4.66.0
pyyaml>=6.0.0

# Note: Removed the following for Streamlit Cloud compatibility:
# - lxml (requires C libraries not available on Streamlit Cloud)
# - requests, beautifulsoup4 (not used in current streamlit app)
# - python-dotenv (not needed for cloud deployment)
#
# If you need web scraping features, consider using:
# - requests-html (pure Python alternative)
# - Or run data collection locally and commit processed CSV files
